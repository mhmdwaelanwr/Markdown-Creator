// MarkdownImporter for React Native (stub)
// Would use a markdown parser like 'marked' or 'remark' for parsing
export function parseMarkdown(markdown) {
  // TODO: Parse markdown and return array of ReadmeElement-like objects
  return [];
}
